# 联调与验收清单

## 1. 契约检查

```text
API 路径已写入 docs/api/api-contract.md
Mock 字段已写入 docs/api/mock-contract.md
数据库表和辅助表已写入 docs/database/database-design.md
SQL 执行顺序已写入 docs/database/sql-execution-and-import.md
前端只使用 lowerCamelCase API 字段
```

## 2. A -> B 数据库接口检查

```text
表名和字段名均为 snake_case
Q1/Q5/Q12/Q14 SQL 可执行
TPC-C New-Order SQL 流程明确
TPC-C Payment SQL 流程明确
stock_change_log 可记录库存变化
索引脚本可执行
```

## 3. B -> C API 检查

```text
登录注册接口可用
用户管理接口可用
导入任务接口可用
TPC-H 接口返回 records/chartData/elapsedMs
TPC-C 接口返回 transactionId/status/elapsedMs
性能结果接口返回 latencySeries/throughputSeries
错误响应 code/message 清楚
```

## 4. C 前端检查

```text
Mock 模式可完整演示
真实接口模式可登录
表格字段和 Mock 字段一致
图表字段和 chartData 一致
导入进度和错误日志可展示
性能图表可展示 D 提供的 JSON/CSV 转换结果
```

## 5. D 环境检查

```text
Docker PostgreSQL 可启动
sql/ 挂载到 /sql
db/init/ 挂载到 /docker-entrypoint-initdb.d
00_run_sql_assets.sh 使用 LF
00_run_sql_assets.sh 有可执行权限
SF=0.1 完整导入
SF=0.6/SF=1 第 4-5 天冻结
行数统计已保存
EXPLAIN ANALYZE 已保存
性能 CSV/JSON 已保存
```

## 6. 验收演示检查

```text
管理员登录
普通用户注册和审批
系统总览显示数据规模
系统演示导入显示成功和失败行
导出功能可触发
业务查询可展示结果
TPC-H 四个查询有表格和图表
触发器和存储过程有证据
索引前后对比有耗时和 EXPLAIN
TPC-C 两个事务可提交，异常可回滚
性能分析页显示平均延迟和吞吐量
```

