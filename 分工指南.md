# 数据库课程设计四人分工指南

项目名称：TPC Benchmark 电商数据管理系统  
技术栈：SpringBoot + Vue 3 + Element Plus + ECharts + PostgreSQL / openGauss + MyBatis / JDBC + Python 多线程性能测试脚本  
建议周期：7 天
目标定位：高完成度、高可演示性、高报告质量、高数据库课程相关度

---

## 0. 项目总目标

本项目不是普通 Web 管理系统，而是一个围绕 TPC-H / TPC-C 基准数据集构建的数据库应用系统。高分目标不是页面做得花哨，而是要完整体现数据库系统设计、数据管理、事务处理、查询优化和性能分析能力。

最终系统应做到：

1. 使用 PostgreSQL 或 openGauss 建立 TPC-H 8 张核心表，并根据 TPC-C 事务需要扩展必要的 OLTP 表。
2. 支持用户登录、注册、管理员审批、角色权限控制。
3. 支持 TPC-H 数据生成、批量导入、数据清洗、错误日志、导入进度展示。
4. 支持至少 3 张表的数据导出，以及查询结果导出。
5. 支持客户信息查询、订单收入查询、零部件供应查询等业务查询。
6. 支持至少 4 条 TPC-H 统计分析查询，展示结果表格、执行时间、图表和执行计划。
7. 支持至少 2 个 TPC-C 事务：New-Order 和 Payment。
8. 支持存储过程、触发器、索引设计、执行计划分析。
9. 支持 TPC-H / TPC-C 并发测试，输出吞吐量、平均延迟、最大延迟、成功率等指标。
10. 形成完整课程设计报告、系统截图、SQL 脚本、测试脚本、性能数据和 README。

---

## 1. 高分原则

### 1.1 每个功能都要形成证据链

每个重要功能不能只写在报告里，必须有可验证证据：

| 功能 | 必须保留的证据 |
|---|---|
| 用户管理 | 登录页面截图、注册页面截图、管理员审批截图、用户表数据截图 |
| 数据导入 | 文件选择截图、导入进度截图、成功/失败统计截图、导入日志文件 |
| 数据清洗 | 人为构造错误数据、错误行日志、合法数据继续导入证明 |
| 触发器 | 触发器 SQL、数据库中触发器存在截图、触发器执行结果截图 |
| 存储过程 | 存储过程 SQL、调用截图、结果截图 |
| 业务查询 | 查询输入截图、结果表格截图、图表截图、导出文件截图 |
| TPC-H 分析 | 查询 SQL、执行时间、结果图表、EXPLAIN ANALYZE 执行计划 |
| 索引优化 | 建索引前后耗时对比、执行计划对比、分析结论 |
| TPC-C 事务 | 事务输入、提交结果、回滚结果、内部 SQL 耗时 |
| 并发测试 | Python 脚本、测试参数、CSV 结果、吞吐量/延迟图表 |
| 报告 | 结构完整、图文对应、每个模块有运行示例 |

### 1.2 项目重点排序

优先级从高到低：

1. 数据库建库、约束、导入、清洗。
2. 用户管理、权限控制。
3. 批量导入、触发器、存储过程。
4. TPC-H 查询分析、执行计划、索引优化。
5. TPC-C 事务处理。
6. 并发测试与性能图表。
7. 页面美化。

不要把大量时间花在 UI 动画、复杂主题切换、无关业务页面上。数据库课程设计的得分重点在数据库能力展示。

---

## 2. 推荐整体架构

### 2.1 系统架构

```text
浏览器客户端
    |
    | Vue 3 + Element Plus + ECharts
    |
SpringBoot 后端服务
    |
    | Controller 层：接收请求、参数校验、权限判断
    | Service 层：业务逻辑、事务控制、性能计时
    | Mapper 层：MyBatis SQL 映射
    |
PostgreSQL / openGauss 数据库
    |
    | TPC-H 表
    | TPC-C 表
    | 用户表
    | 导入日志表
    | 事务日志表
    | 性能测试结果表
    | 触发器 / 存储过程 / 索引

Python 多线程性能测试脚本
    |
    | JDBC / psycopg2 / HTTP API
    |
SpringBoot 或数据库
```

### 2.2 前后端职责边界

| 层次 | 技术 | 职责 |
|---|---|---|
| 前端 | Vue 3 + Element Plus + ECharts | 页面交互、表格展示、图表展示、导入进度、导出按钮 |
| 后端 | SpringBoot | 登录鉴权、业务接口、事务控制、导入导出、执行 SQL、统计耗时 |
| 数据访问 | MyBatis / JDBC | 复杂 SQL、存储过程调用、批量写入、执行计划查询 |
| 数据库 | PostgreSQL / openGauss | 表结构、约束、索引、触发器、存储过程、事务隔离 |
| 性能测试 | Python | 多线程压测、性能指标计算、生成 CSV 和图表数据 |

---

## 3. 四人角色分工总览

建议采用“模块负责人制”，每个人对一个核心评分方向负责，而不是简单划分为前端/后端。

| 成员 | 角色定位 | 主要负责模块 | 核心交付物 |
|---|---|---|---|
| 成员 A | 数据库与数据管理负责人 | 建库建表、TPC-H/TPC-C 表、数据生成、批量导入、数据清洗、触发器、存储过程 | SQL 脚本、导入程序、清洗日志、触发器/存储过程截图 |
| 成员 B | 后端与事务负责人 | SpringBoot 后端、登录鉴权、业务接口、TPC-H 查询接口、TPC-C 事务接口、事务耗时统计 | 后端代码、API 文档、事务提交/回滚截图 |
| 成员 C | 前端与可视化负责人 | Vue 页面、Element Plus 组件、ECharts 图表、导入导出页面、查询页面、性能页面 | 前端代码、系统截图、图表截图、导出结果截图 |
| 成员 D | 性能测试与报告负责人 | 索引优化、执行计划、TPC-H/TPC-C 并发测试、测试结果分析、报告整合、验收演示 | Python 脚本、CSV 数据、性能图表、报告终稿、演示稿 |

### 3.1 共同责任

以下任务四人共同承担：

1. 每天同步进度和阻塞问题。
2. 统一数据库字段命名、接口路径、代码风格。
3. 共同完成系统联调。
4. 共同准备验收演示。
5. 每个人负责自己模块在报告中的文字、截图和代码说明。

---

## 4. 成员 A：数据库与数据管理负责人

### 4.1 角色目标

成员 A 是项目数据库底座负责人，决定项目是否具有“数据库课程设计”的核心价值。A 的工作必须真实、可运行、可截图、可写入报告。

### 4.2 具体任务

#### 4.2.1 数据库环境与建库

1. 选择 PostgreSQL 或 openGauss。
2. 创建项目数据库，例如：

```sql
CREATE DATABASE tpch_ecommerce;
```

3. 创建 TPC-H 8 张表：
   - REGION
   - NATION
   - SUPPLIER
   - PART
   - PARTSUPP
   - CUSTOMER
   - ORDERS
   - LINEITEM

4. 添加主键、外键、非空约束。
5. 创建应用系统辅助表：
   - app_user
   - import_task
   - import_error_log
   - query_log
   - transaction_log
   - stock_change_log
   - performance_result

#### 4.2.2 TPC-H 数据生成

1. 编译或获取 dbgen。
2. 推荐生成规模：

```bash
./dbgen -vf -s 1
```

3. 如果设备性能不足，至少保证不低于 600M。
4. 保存生成过程截图。
5. 保存各表文件大小和数据量统计。

#### 4.2.3 数据导入设计

必须完成两类导入：

1. 基础导入：将 TPC-H 8 张表数据导入数据库。
2. 程序批量导入：至少实现 2 张大表的前后端可操作导入。

推荐程序导入表：

| 表名 | 原因 |
|---|---|
| ORDERS | 订单核心表，字段类型丰富，适合清洗 |
| LINEITEM | 最大表，能体现大规模批量导入 |

如果 LINEITEM 过大导致开发压力过高，可以改为：

| 表名 | 原因 |
|---|---|
| PARTSUPP | 数据量较大，字段较少，适合稳定演示 |

#### 4.2.4 数据清洗规则

至少每张导入表检查两个字段。高分建议更多。

ORDERS 清洗规则：

| 字段 | 清洗规则 |
|---|---|
| O_ORDERKEY | 非空，整数，主键 |
| O_CUSTKEY | 非空，整数，必须能关联 CUSTOMER |
| O_TOTALPRICE | 非空，DECIMAL，且大于等于 0 |
| O_ORDERDATE | 非空，合法日期格式 |
| O_ORDERSTATUS | 非空，限定为 O、F、P 等合法状态 |

LINEITEM 清洗规则：

| 字段 | 清洗规则 |
|---|---|
| L_ORDERKEY | 非空，整数，关联 ORDERS |
| L_LINENUMBER | 非空，整数 |
| L_QUANTITY | 非空，数值，必须大于 0 |
| L_EXTENDEDPRICE | 非空，数值，必须大于等于 0 |
| L_DISCOUNT | 非空，数值，范围 0 到 1 |
| L_SHIPDATE | 非空，合法日期 |

PARTSUPP 清洗规则：

| 字段 | 清洗规则 |
|---|---|
| PS_PARTKEY | 非空，整数，关联 PART |
| PS_SUPPKEY | 非空，整数，关联 SUPPLIER |
| PS_AVAILQTY | 非空，整数，必须大于等于 0 |
| PS_SUPPLYCOST | 非空，数值，必须大于等于 0 |

#### 4.2.5 错误日志设计

建议建表：

```sql
CREATE TABLE import_error_log (
    id BIGSERIAL PRIMARY KEY,
    task_id BIGINT NOT NULL,
    table_name VARCHAR(50) NOT NULL,
    file_name VARCHAR(255),
    line_number BIGINT NOT NULL,
    field_name VARCHAR(100),
    field_value TEXT,
    error_reason TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

日志示例：

```text
[ERROR] table=ORDERS line=1024 field=O_TOTALPRICE value=abc reason=订单总价必须为数值型
[ERROR] table=LINEITEM line=2051 field=L_QUANTITY value=NULL reason=数量不能为空
[ERROR] table=PARTSUPP line=3098 field=PS_AVAILQTY value=-10 reason=供应数量不能小于0
```

#### 4.2.6 触发器设计

至少完成一个触发器。高分建议两个。

触发器 1：导入审计触发器

```text
作用：当导入数据写入目标表时，记录插入或更新行为。
适用表：ORDERS 或 PARTSUPP。
报告价值：证明系统不是普通 insert，而是带有数据库端自动处理逻辑。
```

触发器 2：库存变动触发器

```text
作用：当 TPC-C STOCK 表库存发生变化时，记录 old_quantity、new_quantity、change_time。
适用事务：New-Order。
报告价值：体现 OLTP 事务与数据库触发器结合。
```

#### 4.2.7 存储过程设计

至少完成一个存储过程。高分建议两个。

存储过程 1：地区收入统计

```text
输入：region_name, start_date, end_date
输出：国家名称、收入总额
对应 TPC-H Q5。
```

存储过程 2：运送方式与订单优先级统计

```text
输入：start_date, end_date, ship_mode1, ship_mode2
输出：shipmode、高优先级订单数、低优先级订单数
对应 TPC-H Q12。
```

#### 4.2.8 A 的最终交付物

```text
sql/
  01_create_database.sql
  02_create_tpch_tables.sql
  03_create_tpcc_tables.sql
  04_constraints.sql
  05_indexes_initial.sql
  06_triggers.sql
  07_procedures.sql
  08_seed_app_user.sql

docs/screenshots/database/
  dbgen_success.png
  table_row_count.png
  constraint_result.png
  trigger_exists.png
  procedure_call_result.png
  import_error_log.png

report/A_数据库设计与数据管理.md
```

---

## 5. 成员 B：后端与事务负责人

### 5.1 角色目标

成员 B 负责 SpringBoot 后端系统，保证前端所有页面都有稳定接口，数据库操作可控，TPC-C 事务符合 ACID 思想，且每个关键操作都能记录执行时间。

### 5.2 SpringBoot 项目结构

推荐后端目录：

```text
backend/
  src/main/java/com/example/tpch/
    TpchApplication.java
    config/
      SecurityConfig.java
      MyBatisConfig.java
      WebConfig.java
    controller/
      AuthController.java
      UserController.java
      SystemController.java
      ImportController.java
      ExportController.java
      BusinessQueryController.java
      TpchAnalysisController.java
      TpccTransactionController.java
      PerformanceController.java
    service/
      AuthService.java
      UserService.java
      ImportService.java
      ExportService.java
      BusinessQueryService.java
      TpchAnalysisService.java
      TpccTransactionService.java
      PerformanceService.java
    mapper/
      UserMapper.java
      SystemMapper.java
      ImportMapper.java
      ExportMapper.java
      BusinessQueryMapper.java
      TpchAnalysisMapper.java
      TpccMapper.java
      PerformanceMapper.java
    entity/
    dto/
    vo/
    common/
      Result.java
      PageResult.java
      BusinessException.java
      GlobalExceptionHandler.java
  src/main/resources/
    application.yml
    mapper/
```

### 5.3 用户认证与权限接口

必须实现：

| 接口 | 方法 | 功能 |
|---|---|---|
| /api/auth/login | POST | 用户登录 |
| /api/auth/register | POST | 普通用户注册 |
| /api/auth/logout | POST | 退出登录 |
| /api/user/list | GET | 管理员查看用户列表 |
| /api/user/approve/{id} | POST | 管理员审批用户 |
| /api/user/disable/{id} | POST | 管理员禁用用户 |
| /api/user/delete/{id} | DELETE | 管理员删除用户 |

用户角色：

```text
admin：系统管理员
user：普通用户
pending：待审批用户
```

权限要求：

| 功能 | admin | user |
|---|---|---|
| 系统管理 | 是 | 否 |
| 用户审批 | 是 | 否 |
| 数据导入 | 是 | 否，或只读 |
| 数据导出 | 是 | 是 |
| 业务查询 | 是 | 是 |
| TPC-H 分析 | 是 | 是 |
| TPC-C 事务 | 是 | 可选 |
| 索引管理 | 是 | 否 |
| 并发测试 | 是 | 否 |

### 5.4 数据导入接口

| 接口 | 方法 | 功能 |
|---|---|---|
| /api/import/tables | GET | 获取可导入表列表 |
| /api/import/start | POST | 开始导入任务 |
| /api/import/progress/{taskId} | GET | 查询导入进度 |
| /api/import/errors/{taskId} | GET | 查询错误日志 |
| /api/import/tasks | GET | 查询历史导入任务 |

注意：

1. 导入不能一次性把整个文件读入内存。
2. 后端必须支持分批读取。
3. 合法数据批量写入数据库。
4. 非法数据写入 import_error_log。
5. 返回给前端总行数、成功行数、失败行数、进度百分比、耗时。

### 5.5 数据导出接口

| 接口 | 方法 | 功能 |
|---|---|---|
| /api/export/tables | GET | 获取可导出表 |
| /api/export/table | GET | 导出指定表 |
| /api/export/query-result | POST | 导出查询结果 |

导出格式：

```text
CSV：实现最快，最稳定。
Excel：加分，但不是必须。
```

### 5.6 业务查询接口

| 接口 | 方法 | 功能 |
|---|---|---|
| /api/query/customers | GET | 客户信息查询 |
| /api/query/customer-options | GET | 客户姓名/国家下拉选项 |
| /api/query/order-revenue | GET | 订单收入查询 |
| /api/query/part-supplier | GET | 零部件供应查询 |

每个查询必须返回：

```json
{
  "records": [],
  "chartData": {},
  "elapsedMs": 123,
  "total": 100
}
```

### 5.7 TPC-H 统计分析接口

建议实现 4 条：

| 接口 | 对应查询 | 功能 |
|---|---|---|
| /api/tpch/q1 | Q1 | 定价汇总报表 |
| /api/tpch/q5 | Q5 | 地区收入分析 |
| /api/tpch/q12 | Q12 | 运送方式与订单优先级 |
| /api/tpch/q14 | Q14 | 促销效果分析 |

每个接口返回：

```json
{
  "queryName": "TPC-H Q5 本地供应商收入量查询",
  "params": {},
  "elapsedMs": 5321,
  "records": [],
  "chartData": {},
  "explainPlan": "...",
  "rowCount": 5
}
```

### 5.8 TPC-C 事务接口

必须实现：

| 接口 | 方法 | 功能 |
|---|---|---|
| /api/tpcc/new-order | POST | 新订单事务 |
| /api/tpcc/payment | POST | 支付事务 |
| /api/tpcc/transaction-log | GET | 查看事务日志 |

New-Order 事务流程：

```text
BEGIN
1. 查询 WAREHOUSE 税率
2. 查询 CUSTOMER 折扣和信用状态
3. 查询 DISTRICT 当前 D_NEXT_O_ID
4. 更新 D_NEXT_O_ID = D_NEXT_O_ID + 1
5. 插入 ORDERS
6. 插入 NEW_ORDER
7. 循环处理订单行：
   7.1 查询 ITEM 价格
   7.2 查询 STOCK 库存
   7.3 更新 STOCK 数量
   7.4 插入 ORDER_LINE
8. 计算订单总金额
COMMIT
异常：ROLLBACK
```

Payment 事务流程：

```text
BEGIN
1. 更新 WAREHOUSE 的 W_YTD
2. 更新 DISTRICT 的 D_YTD
3. 查询 CUSTOMER
4. 更新 CUSTOMER 余额、支付次数、累计支付额
5. 插入 HISTORY
COMMIT
异常：ROLLBACK
```

事务耗时记录要求：

```text
transaction_id
transaction_type
start_time
end_time
elapsed_ms
status
rollback_reason
sql_step_timings
```

### 5.9 后端异常处理

必须统一异常返回格式：

```json
{
  "code": 500,
  "message": "订单总价必须为数值型",
  "data": null
}
```

避免前端因为后端异常直接白屏。

### 5.10 B 的最终交付物

```text
backend/
  完整 SpringBoot 代码
  application.yml.example
  README_backend.md

docs/api/
  api接口说明.md

docs/screenshots/backend/
  swagger_or_postman.png
  transaction_success.png
  transaction_rollback.png
  query_elapsed_time.png

report/B_后端业务与事务实现.md
```

---

## 6. 成员 C：前端与可视化负责人

### 6.1 角色目标

成员 C 负责让系统具有完整、清晰、可演示的界面。前端不需要炫技，但必须结构规范、布局清楚、图表直观、操作顺畅。

### 6.2 Vue 项目结构

推荐目录：

```text
frontend/
  src/
    api/
      auth.js
      user.js
      system.js
      import.js
      export.js
      query.js
      tpch.js
      tpcc.js
      performance.js
    router/
      index.js
    store/
      user.js
    views/
      Login.vue
      Register.vue
      layout/
        MainLayout.vue
      dashboard/
        Dashboard.vue
      user/
        UserList.vue
        UserApprove.vue
      system/
        DbInfo.vue
        TableInfo.vue
      data/
        DataImport.vue
        ImportLog.vue
        DataExport.vue
        TablePreview.vue
      query/
        CustomerQuery.vue
        OrderRevenueQuery.vue
        PartSupplierQuery.vue
      tpch/
        Q1Summary.vue
        Q5Revenue.vue
        Q12ShipMode.vue
        Q14Promo.vue
      tpcc/
        NewOrder.vue
        Payment.vue
        TransactionLog.vue
      performance/
        IndexCompare.vue
        ExplainPlan.vue
        ConcurrentTest.vue
    components/
      QueryResultTable.vue
      ChartCard.vue
      ImportProgress.vue
      ExplainPlanViewer.vue
```

### 6.3 菜单设计

建议一级菜单和二级菜单：

```text
1. 系统首页
   1.1 数据库总览
   1.2 表规模统计

2. 系统管理
   2.1 数据库连接信息
   2.2 数据表信息
   2.3 系统运行日志

3. 用户管理
   3.1 用户列表
   3.2 用户审批

4. 数据管理
   4.1 TPC-H 数据导入
   4.2 数据清洗日志
   4.3 数据导出
   4.4 表数据预览

5. 业务查询
   5.1 客户信息查询
   5.2 订单收入查询
   5.3 零部件供应查询

6. TPC-H 统计分析
   6.1 Q1 定价汇总报表
   6.2 Q5 地区收入分析
   6.3 Q12 运送方式与订单优先级
   6.4 Q14 促销效果分析

7. TPC-C 事务处理
   7.1 New-Order 事务
   7.2 Payment 事务
   7.3 事务日志

8. 性能分析
   8.1 索引优化对比
   8.2 查询执行计划
   8.3 并发测试结果
```

### 6.4 页面实现要求

#### 6.4.1 登录页

必须包含：

1. 用户名。
2. 密码。
3. 登录按钮。
4. 注册入口。
5. 登录失败提示。

#### 6.4.2 注册页

必须包含：

1. 用户名。
2. 密码。
3. 确认密码。
4. 注册后状态为待审批。
5. 提示“请等待管理员审批”。

#### 6.4.3 系统首页 Dashboard

建议展示：

| 卡片 | 内容 |
|---|---|
| 数据库类型 | PostgreSQL / openGauss |
| TPC-H 规模 | SF=1 或 SF=0.6 |
| 表总数 | TPC-H 8 张 + TPC-C 扩展表 + 应用表 |
| 总数据量 | 行数统计 |
| 最近导入任务 | 成功/失败数量 |
| 最近查询平均耗时 | ms |

图表：

1. 各表行数柱状图。
2. 最近查询耗时折线图。
3. 导入成功/失败饼图。

#### 6.4.4 数据导入页面

必须包含：

1. 表名下拉框。
2. 文件选择。
3. 批次大小输入。
4. 开始导入按钮。
5. 进度条。
6. 当前读取行数。
7. 成功行数。
8. 失败行数。
9. 导入耗时。
10. 查看错误日志按钮。

#### 6.4.5 数据导出页面

必须包含：

1. 表名下拉框。
2. 导出格式选择。
3. 导出按钮。
4. 导出成功提示。
5. 下载链接。

#### 6.4.6 客户查询页面

必须包含：

1. 输入框查询。
2. 下拉框查询。
3. 国家筛选。
4. 查询按钮。
5. 结果表格。
6. 导出结果按钮。

#### 6.4.7 TPC-H 统计分析页面

每个 TPC-H 页面建议统一结构：

```text
顶部：查询说明 + 参数输入
中部：执行按钮 + 执行时间 + 返回行数
下部：结果表格 + ECharts 图表
底部：EXPLAIN ANALYZE 执行计划 + 导出按钮
```

#### 6.4.8 TPC-C 事务页面

New-Order 页面：

1. 仓库 ID。
2. 地区 ID。
3. 客户 ID。
4. 商品列表动态增删。
5. 每个商品包含 item_id、supply_warehouse_id、quantity。
6. 提交事务按钮。
7. 显示订单总金额、事务耗时、提交状态、库存变化。

Payment 页面：

1. 仓库 ID。
2. 地区 ID。
3. 客户 ID 或客户姓氏。
4. 支付金额。
5. 提交事务按钮。
6. 显示旧余额、新余额、事务耗时、提交状态。

#### 6.4.9 性能分析页面

至少包含：

1. 索引前后执行时间对比柱状图。
2. TPC-H 并发线程数与平均延迟折线图。
3. TPC-H 并发线程数与 QPS 柱状图。
4. TPC-C 并发线程数与 TPS 柱状图。
5. 执行计划文本展示区。

### 6.5 ECharts 图表建议

| 场景 | 图表类型 |
|---|---|
| 各表行数 | 柱状图 |
| 国家收入 | 柱状图 |
| 订单收入随时间变化 | 折线图 |
| 促销收入占比 | 饼图或仪表盘 |
| 运送方式高低优先级 | 堆叠柱状图 |
| 索引前后耗时 | 对比柱状图 |
| 并发线程数与响应时间 | 折线图 |
| 并发线程数与吞吐量 | 柱状图 |

### 6.6 C 的最终交付物

```text
frontend/
  完整 Vue 3 代码
  README_frontend.md

docs/screenshots/frontend/
  login.png
  register.png
  dashboard.png
  user_approve.png
  data_import.png
  import_progress.png
  import_error_log.png
  data_export.png
  customer_query.png
  tpch_q1_chart.png
  tpch_q5_chart.png
  tpcc_new_order.png
  performance_index_compare.png
  performance_concurrent.png

report/C_前端交互与可视化实现.md
```

---

## 7. 成员 D：性能测试与报告负责人

### 7.1 角色目标

成员 D 负责把系统从“能跑”提升到“有实验分析、有性能结论、有高分报告”。D 的工作决定报告质量和验收表达效果。

### 7.2 索引优化实验

至少选择 3 条查询进行索引前后对比：

| 查询 | 优化点 |
|---|---|
| Q1 定价汇总报表 | LINEITEM 的 L_SHIPDATE、L_RETURNFLAG、L_LINESTATUS |
| Q5 地区收入分析 | ORDERS 日期、CUSTOMER/SUPPLIER 国家、LINEITEM 连接键 |
| Q12 运送方式与订单优先级 | LINEITEM 运送方式、收货日期、ORDERS 优先级 |

#### 7.2.1 实验流程

每条查询都按以下步骤：

```text
1. 删除相关非主键索引。
2. 执行 EXPLAIN ANALYZE，记录执行计划和耗时。
3. 创建针对性索引。
4. 再次执行 EXPLAIN ANALYZE，记录执行计划和耗时。
5. 对比 Seq Scan / Index Scan / Bitmap Index Scan / Hash Join / Nested Loop 的变化。
6. 写出是否优化成功，以及原因。
```

#### 7.2.2 结果表模板

| 查询 | 索引状态 | 执行时间 ms | 主要执行方式 | 是否使用索引 | 说明 |
|---|---|---:|---|---|---|
| Q1 | 无索引 |  | Seq Scan | 否 | 大表扫描 |
| Q1 | 有索引 |  | Bitmap Index Scan | 是 | 日期过滤加速 |
| Q5 | 无索引 |  | Hash Join | 部分否 | 多表连接耗时较高 |
| Q5 | 有索引 |  | Index Scan + Hash Join | 是 | 连接键索引生效 |

注意：如果某条查询建索引后没有明显变快，也可以成为高分分析点。需要说明原因，例如数据选择率低、返回数据量大、优化器认为顺序扫描成本更低。

### 7.3 TPC-H 并发测试脚本

建议用 Python 多线程，通过 HTTP 调用后端接口，或者直接用 psycopg2 连接数据库执行 SQL。

测试参数：

```text
线程数：1, 2, 4, 8, 16
每个线程执行次数：5 或 10
查询集合：Q1, Q5, Q12, Q14
```

记录指标：

```text
thread_count
query_name
total_requests
success_count
fail_count
avg_latency_ms
max_latency_ms
min_latency_ms
p95_latency_ms
qps
start_time
end_time
```

输出文件：

```text
test/results/tpch_concurrent_result.csv
```

### 7.4 TPC-C 并发测试脚本

测试参数：

```text
线程数：1, 2, 4, 8
事务类型：New-Order, Payment
事务比例：50% / 50%
每个线程执行事务数：10 或 20
```

记录指标：

```text
thread_count
transaction_type
total_transactions
commit_count
rollback_count
avg_latency_ms
max_latency_ms
min_latency_ms
p95_latency_ms
tps
```

输出文件：

```text
test/results/tpcc_concurrent_result.csv
```

### 7.5 性能分析结论模板

TPC-H 分析可以这样写：

```text
随着并发线程数从 1 增加到 8，系统吞吐量逐步提升，说明数据库能够利用连接池和多核资源并行处理查询请求。当线程数继续增加到 16 时，平均响应时间明显上升，吞吐量增长趋缓，说明系统瓶颈逐渐转移到大表扫描、连接计算和数据库 I/O。
```

TPC-C 分析可以这样写：

```text
TPC-C 事务包含多条写操作，尤其 New-Order 事务会更新 DISTRICT 的下一个订单号和 STOCK 库存数量。随着并发数增加，热点数据上的锁竞争增强，因此平均响应时间上升，部分事务出现等待或回滚。该现象符合 OLTP 系统在高并发写入场景下的典型表现。
```

索引分析可以这样写：

```text
对于选择率较高的日期过滤条件，索引能够有效减少扫描行数，使查询计划从顺序扫描转为 Bitmap Index Scan 或 Index Scan。但对于返回大量数据并需要聚集计算的查询，优化器可能仍选择顺序扫描，因为顺序扫描的 I/O 成本低于大量随机索引访问。
```

### 7.6 报告整合任务

D 负责最终报告结构，但每个成员要提交自己模块内容。

报告建议目录：

```text
1. 课程设计目的与内容
2. 系统需求分析
3. 系统总体设计
4. 数据库设计
5. 数据批量导入与清洗设计
6. 存储过程与触发器设计
7. 系统功能实现
8. TPC-H 统计分析实现
9. TPC-C 事务处理实现
10. 索引设计与查询执行计划分析
11. 并发测试与性能分析
12. 系统运行示例
13. 小组分工
14. 总结与改进方向
15. 附录：核心 SQL、代码片段、测试结果
```

### 7.7 D 的最终交付物

```text
test/
  tpch_concurrent_test.py
  tpcc_concurrent_test.py
  requirements.txt
  results/
    tpch_concurrent_result.csv
    tpcc_concurrent_result.csv
    index_compare_result.csv

docs/screenshots/performance/
  q1_explain_before.png
  q1_explain_after.png
  q5_explain_before.png
  q5_explain_after.png
  q12_explain_before.png
  q12_explain_after.png
  tpch_latency_chart.png
  tpch_qps_chart.png
  tpcc_tps_chart.png

report/
  课程设计报告.docx
  D_性能测试与报告整合.md
  验收演示稿.md
```

---

## 8. 任务依赖关系

### 8.1 关键依赖图

```text
A 建库建表
    ↓
B 后端数据库连接
    ↓
C 前端调用接口
    ↓
D 性能测试接口

A 数据导入完成
    ↓
B TPC-H 查询接口
    ↓
C 统计分析页面
    ↓
D 索引优化与并发测试

A TPC-C 表准备
    ↓
B TPC-C 事务实现
    ↓
C TPC-C 页面
    ↓
D TPC-C 并发测试
```

### 8.2 必须先完成的阻塞项

| 阻塞项 | 负责人 | 影响范围 | 最晚完成时间 |
|---|---|---|---|
| 数据库建表 | A | 全项目 | 第 1 天晚 |
| 后端连接数据库 | B | 前端、测试 | 第 1 天晚 |
| 登录权限 | B/C | 用户管理、菜单权限 | 第 2 天晚 |
| TPC-H 数据导入 | A | 查询分析、索引优化 | 第 2 天晚 |
| 导入接口 | B | 前端导入页面 | 第 3 天中 |
| TPC-H 查询接口 | B | 图表与性能分析 | 第 4 天中 |
| TPC-C 表与数据 | A | TPC-C 事务 | 第 5 天早 |
| TPC-C 事务接口 | B | TPC-C 页面和测试 | 第 5 天晚 |
| 并发测试数据 | D | 报告性能分析 | 第 6 天晚 |

---

## 9. 七天高分排期

### 第 1 天：项目骨架与数据库底座

| 成员 | 当天任务 | 当天交付物 |
|---|---|---|
| A | 安装 PostgreSQL/openGauss；创建数据库；创建 TPC-H 8 表；创建应用辅助表 | schema.sql、建表成功截图 |
| B | 创建 SpringBoot 项目；配置 MyBatis/JDBC；实现数据库连通测试 | 后端项目、健康检查接口 |
| C | 创建 Vue 3 项目；配置 Element Plus、ECharts、路由、布局 | 前端项目、登录页草稿、主布局 |
| D | 创建 Git 仓库规范；建立报告目录；整理验收清单；设计性能测试指标 | README 初稿、报告目录、任务看板 |

验收标准：

```text
1. 数据库能连接。
2. 后端能启动。
3. 前端能启动。
4. Git 仓库结构确定。
5. 四人均能本地运行项目。
```

### 第 2 天：数据生成、用户系统、系统首页

| 成员 | 当天任务 | 当天交付物 |
|---|---|---|
| A | 使用 dbgen 生成 SF=1 或 SF=0.6 数据；导入 TPC-H 基础表；统计各表行数 | 数据文件、导入截图、行数统计 |
| B | 实现登录、注册、Token、角色权限、用户审批接口 | AuthController、UserController |
| C | 完成登录页、注册页、首页 Dashboard、用户管理页面 | 页面截图 |
| D | 写需求分析初稿；画系统架构图；记录环境配置步骤 | 需求分析.md、架构图 |

验收标准：

```text
1. 管理员能登录。
2. 普通用户能注册并等待审批。
3. 管理员能审批用户。
4. Dashboard 能显示表行数。
```

### 第 3 天：批量导入、数据清洗、导出

| 成员 | 当天任务 | 当天交付物 |
|---|---|---|
| A | 实现 ORDERS 和 LINEITEM/PARTSUPP 清洗规则；构造错误数据；写触发器 | 清洗规则、错误日志、触发器 SQL |
| B | 实现导入接口、进度查询接口、错误日志接口、导出接口 | ImportController、ExportController |
| C | 完成数据导入页面、进度条、错误日志页面、数据导出页面 | 导入/导出页面截图 |
| D | 记录导入实验数据；写批量导入设计章节初稿 | 导入性能表、报告章节 |

验收标准：

```text
1. 可以选择表和文件导入。
2. 导入过程显示进度。
3. 错误数据能被识别并写入日志。
4. 合法数据继续导入。
5. 至少 3 张表可导出。
```

### 第 4 天：业务查询与 TPC-H 统计分析

| 成员 | 当天任务 | 当天交付物 |
|---|---|---|
| A | 提供 Q1/Q5/Q12/Q14 SQL；创建统计存储过程 | tpch_queries.sql、procedure.sql |
| B | 实现客户查询、订单收入查询、零部件供应查询、TPC-H 查询接口 | QueryController、TpchAnalysisController |
| C | 完成客户查询页、订单收入页、零部件查询页、Q1/Q5/Q12/Q14 图表页 | 查询和图表截图 |
| D | 记录每条查询耗时；保存执行计划初版 | 查询耗时表、执行计划文本 |

验收标准：

```text
1. 客户查询同时支持输入框和下拉框。
2. 至少 3 个业务查询能运行。
3. 至少 4 条 TPC-H 查询能运行。
4. 查询结果有表格和图表。
5. 查询结果可导出。
```

### 第 5 天：TPC-C 事务与索引优化

| 成员 | 当天任务 | 当天交付物 |
|---|---|---|
| A | 创建 TPC-C 必要表；准备测试数据；完成库存触发器 | tpcc_tables.sql、stock_trigger.sql |
| B | 实现 New-Order 和 Payment 事务；记录内部 SQL 耗时；支持 rollback | TpccTransactionService |
| C | 完成 New-Order 页面、Payment 页面、事务日志页面 | 事务页面截图 |
| D | 执行 Q1/Q5/Q12 索引前后对比；保存 EXPLAIN ANALYZE | 索引对比表、执行计划截图 |

验收标准：

```text
1. New-Order 能提交成功。
2. New-Order 异常时能回滚。
3. Payment 能提交成功。
4. 事务日志能显示耗时和状态。
5. 至少 3 条查询有索引前后对比。
```

### 第 6 天：并发测试、性能图表、报告初稿

| 成员 | 当天任务 | 当天交付物 |
|---|---|---|
| A | 配合解决数据库锁、索引、慢查询问题 | 优化后的 SQL / 索引 |
| B | 提供性能测试所需接口；修复后端 bug | 稳定后端版本 |
| C | 完成性能分析页面；接入并发测试结果图表 | 性能图表页面 |
| D | 编写并运行 TPC-H/TPC-C 并发测试脚本；整合报告初稿 | CSV 结果、报告初稿 |

验收标准：

```text
1. TPC-H 并发测试有结果。
2. TPC-C 并发测试有结果。
3. 前端能展示吞吐量和延迟图。
4. 报告初稿完成 80%。
5. 系统能完整演示一遍。
```

### 第 7 天：联调、报告、验收准备

| 成员 | 当天任务 | 当天交付物 |
|---|---|---|
| A | 检查所有 SQL 脚本；整理数据库初始化说明 | sql 文件夹、数据库说明 |
| B | 检查后端启动说明；整理接口说明；修复最后 bug | README_backend.md |
| C | 检查所有页面；补齐截图；统一界面风格 | 截图文件夹、README_frontend.md |
| D | 完成报告终稿；准备演示稿；打包提交文件 | 报告 docx、演示稿、提交包 |

验收标准：

```text
1. 报告完整。
2. 代码可运行。
3. SQL 脚本完整。
4. README 清楚。
5. 演示流程稳定。
6. 有备用截图。
```

---

## 10. Git 仓库规范

### 10.1 推荐目录结构

```text
tpch-ecommerce-system/
  backend/
  frontend/
  sql/
    01_create_database.sql
    02_create_tpch_tables.sql
    03_create_tpcc_tables.sql
    04_constraints.sql
    05_indexes.sql
    06_triggers.sql
    07_procedures.sql
    08_seed_data.sql
  test/
    tpch_concurrent_test.py
    tpcc_concurrent_test.py
    requirements.txt
    results/
  docs/
    screenshots/
      database/
      frontend/
      backend/
      performance/
    api/
    diagrams/
    import_logs/
    explain_plans/
  report/
    parts/
      A_数据库设计与数据管理.md
      B_后端业务与事务实现.md
      C_前端交互与可视化实现.md
      D_性能测试与报告整合.md
    课程设计报告.docx
    验收演示稿.md
  README.md
```

### 10.2 分支建议

```text
main：稳定可演示版本
dev：日常集成版本
feature/database：成员 A
feature/backend：成员 B
feature/frontend：成员 C
feature/performance-report：成员 D
```

每天至少合并一次到 dev。第 6 天晚上冻结功能，只修 bug，不再大改架构。

### 10.3 Commit 规范

```text
feat: 完成用户登录接口
fix: 修复导入进度计算错误
sql: 添加 LINEITEM 索引脚本
docs: 添加批量导入设计说明
test: 添加 TPC-H 并发测试脚本
refactor: 优化 TPC-C 事务服务结构
```

---

## 11. 数据库表设计建议

### 11.1 应用用户表

```sql
CREATE TABLE app_user (
    user_id BIGSERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    role VARCHAR(20) NOT NULL CHECK (role IN ('admin', 'user')),
    status VARCHAR(20) NOT NULL CHECK (status IN ('pending', 'approved', 'disabled')),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 11.2 导入任务表

```sql
CREATE TABLE import_task (
    task_id BIGSERIAL PRIMARY KEY,
    table_name VARCHAR(50) NOT NULL,
    file_name VARCHAR(255) NOT NULL,
    total_rows BIGINT DEFAULT 0,
    success_rows BIGINT DEFAULT 0,
    failed_rows BIGINT DEFAULT 0,
    status VARCHAR(20) NOT NULL,
    start_time TIMESTAMP,
    end_time TIMESTAMP,
    elapsed_ms BIGINT,
    created_by BIGINT
);
```

### 11.3 查询日志表

```sql
CREATE TABLE query_log (
    log_id BIGSERIAL PRIMARY KEY,
    query_name VARCHAR(100) NOT NULL,
    query_type VARCHAR(50),
    params TEXT,
    elapsed_ms BIGINT,
    row_count BIGINT,
    executed_by BIGINT,
    executed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 11.4 事务日志表

```sql
CREATE TABLE transaction_log (
    tx_id BIGSERIAL PRIMARY KEY,
    tx_type VARCHAR(50) NOT NULL,
    status VARCHAR(20) NOT NULL,
    elapsed_ms BIGINT,
    step_timings TEXT,
    rollback_reason TEXT,
    created_by BIGINT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 11.5 库存变动日志表

```sql
CREATE TABLE stock_change_log (
    id BIGSERIAL PRIMARY KEY,
    s_i_id INTEGER,
    s_w_id INTEGER,
    old_quantity INTEGER,
    new_quantity INTEGER,
    change_type VARCHAR(50),
    changed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 11.6 性能测试结果表

```sql
CREATE TABLE performance_result (
    id BIGSERIAL PRIMARY KEY,
    test_type VARCHAR(50) NOT NULL,
    thread_count INTEGER,
    total_count INTEGER,
    success_count INTEGER,
    fail_count INTEGER,
    avg_latency_ms NUMERIC(12, 2),
    max_latency_ms NUMERIC(12, 2),
    min_latency_ms NUMERIC(12, 2),
    p95_latency_ms NUMERIC(12, 2),
    throughput NUMERIC(12, 2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 12. API 规范建议

### 12.1 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 12.2 分页响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [],
    "total": 1000,
    "page": 1,
    "pageSize": 20
  }
}
```

### 12.3 查询分析响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "queryName": "TPC-H Q5 本地供应商收入分析",
    "elapsedMs": 3521,
    "rowCount": 5,
    "records": [],
    "chartData": {},
    "explainPlan": "..."
  }
}
```

---

## 13. 高分验收演示顺序

演示时间建议控制在 8 到 12 分钟。

```text
1. 登录系统
   展示管理员登录，说明用户口令与数据库口令分离。

2. 用户管理
   展示普通用户注册、管理员审批、权限差异。

3. 系统首页
   展示数据库类型、TPC-H 数据规模、各表行数、系统总览图表。

4. 数据导入
   选择 ORDERS 或 PARTSUPP 文件，设置批次大小，开始导入，展示进度条。

5. 数据清洗日志
   展示人为构造的错误行被识别，合法数据继续导入。

6. 数据导出
   选择 CUSTOMER / ORDERS / LINEITEM 中任意 3 张表导出。

7. 业务查询
   展示客户查询，必须同时展示输入框查询和下拉框查询。

8. TPC-H 统计分析
   展示 Q1、Q5、Q12 或 Q14，说明执行时间、结果表格和图表。

9. 执行计划与索引优化
   展示建索引前后执行时间、EXPLAIN ANALYZE 变化、对比图。

10. TPC-C 事务
   展示 New-Order 提交成功、Payment 提交成功；最好演示一次异常回滚。

11. 并发测试
   展示 TPC-H/TPC-C 多线程测试结果，说明吞吐量和平均延迟变化。

12. 总结亮点
   强调大规模数据、批量导入清洗、触发器、存储过程、事务、索引、并发性能分析。
```

---

## 14. 每日例会模板

每天晚上固定 15 分钟同步。

每个人回答 4 个问题：

```text
1. 今天完成了什么？
2. 明天准备完成什么？
3. 当前阻塞是什么？
4. 需要谁配合？
```

每日必须更新：

```text
1. Git 提交。
2. 任务看板。
3. 截图证据。
4. 报告对应章节。
```

---

## 15. 风险控制

### 15.1 数据库风险

| 风险 | 解决方案 |
|---|---|
| openGauss 安装复杂 | 备用 PostgreSQL |
| 1GB 数据导入太慢 | 先用 0.1 调试，再用 0.6 或 1 正式导入 |
| LINEITEM 太大导入不稳定 | 程序导入改为 ORDERS + PARTSUPP，LINEITEM 用数据库工具导入 |
| 外键导致导入顺序错误 | 先导入基础表，再添加外键约束 |

### 15.2 后端风险

| 风险 | 解决方案 |
|---|---|
| Spring Security 配置耗时 | 简化为拦截器 + Token |
| MyBatis 复杂 SQL 难维护 | 大 SQL 单独放 XML 文件 |
| TPC-C 事务太复杂 | 只实现 New-Order 和 Payment 的核心路径 |
| 文件导入阻塞接口 | 异步任务 + task_id 查询进度 |

### 15.3 前端风险

| 风险 | 解决方案 |
|---|---|
| 页面过多做不完 | 优先做必演示页面 |
| ECharts 数据格式对不上 | 后端统一返回 chartData |
| 导入进度实时性不足 | 用定时轮询 task_id |
| UI 不统一 | 使用 Element Plus 默认风格 |

### 15.4 性能测试风险

| 风险 | 解决方案 |
|---|---|
| 并发测试压垮数据库 | 从 1、2、4 线程逐步测试 |
| 结果波动大 | 每组测试跑 3 次取平均 |
| 索引优化不明显 | 如实分析选择率和执行计划原因 |
| EXPLAIN 截图不清晰 | 保存文本结果，报告中贴关键片段 |

---

## 16. 最终提交包清单

最终建议提交：

```text
班级_姓名1_姓名2_姓名3_姓名4_课程设计报告.docx

source_code/
  backend/
  frontend/

sql/
  01_create_database.sql
  02_create_tpch_tables.sql
  03_create_tpcc_tables.sql
  04_constraints.sql
  05_indexes.sql
  06_triggers.sql
  07_procedures.sql
  08_seed_data.sql

test/
  tpch_concurrent_test.py
  tpcc_concurrent_test.py
  requirements.txt
  results/
    tpch_concurrent_result.csv
    tpcc_concurrent_result.csv
    index_compare_result.csv

docs/
  screenshots/
  api/
  diagrams/
  import_logs/
  explain_plans/

README.md
```

README 必须说明：

```text
1. 项目简介
2. 技术栈
3. 环境要求
4. 数据库初始化步骤
5. 后端启动步骤
6. 前端启动步骤
7. Python 测试脚本运行方法
8. 测试账号
9. 功能演示顺序
10. 常见问题
```

---

## 17. 报告中小组分工写法

可以直接写入报告封面或分工章节：

| 成员 | 分工内容 |
|---|---|
| 成员 A | 负责数据库设计与数据管理模块，包括 TPC-H/TPC-C 表结构设计、完整性约束设计、测试数据生成、数据批量导入、数据清洗规则、导入日志、触发器和存储过程设计。 |
| 成员 B | 负责后端业务逻辑模块，包括 SpringBoot 项目搭建、数据库连接、用户认证与权限控制、业务查询接口、TPC-H 统计分析接口、TPC-C New-Order 和 Payment 事务实现。 |
| 成员 C | 负责前端交互与可视化模块，包括 Vue 3 项目搭建、Element Plus 页面设计、系统菜单、数据导入导出页面、业务查询页面、ECharts 图表展示和查询结果导出。 |
| 成员 D | 负责性能测试与报告整合，包括索引优化实验、查询执行计划分析、TPC-H/TPC-C 并发测试、性能指标统计、系统截图整理、课程设计报告撰写与验收演示准备。 |

共同完成：系统需求分析、系统联调、测试数据构造、验收演示、最终报告修改与代码打包。

---

## 18. 高分检查表

提交前逐项检查。

### 18.1 功能检查

- [ ] 管理员可以登录。
- [ ] 普通用户可以注册。
- [ ] 管理员可以审批普通用户。
- [ ] 普通用户和管理员权限不同。
- [ ] Dashboard 显示数据库和表规模信息。
- [ ] 至少 8 张 TPC-H 表已创建。
- [ ] TPC-H 数据规模不低于 600M，最好 SF=1。
- [ ] 至少 2 张表支持程序批量导入。
- [ ] 数据导入有进度条。
- [ ] 数据导入有错误日志。
- [ ] 数据清洗能识别类型错误、范围错误、空值错误。
- [ ] 至少 3 张表支持导出。
- [ ] 客户查询支持输入框和下拉框。
- [ ] 至少 3 个业务查询能运行。
- [ ] 至少 4 条 TPC-H 分析查询能运行。
- [ ] 查询结果有表格和 ECharts 图表。
- [ ] 查询结果可导出。
- [ ] 至少 1 个触发器真实存在并被调用。
- [ ] 至少 1 个存储过程真实存在并被调用。
- [ ] New-Order 事务能提交。
- [ ] Payment 事务能提交。
- [ ] TPC-C 异常场景能回滚。
- [ ] 至少 3 条查询完成索引前后对比。
- [ ] 有 EXPLAIN ANALYZE 执行计划。
- [ ] 有 TPC-H 并发测试结果。
- [ ] 有 TPC-C 并发测试结果。
- [ ] 有吞吐量、延迟、成功率图表。

### 18.2 文档检查

- [ ] 报告有封面。
- [ ] 报告有目录。
- [ ] 报告有课程设计目的与内容。
- [ ] 报告有需求分析。
- [ ] 报告有系统架构设计。
- [ ] 报告有数据库设计。
- [ ] 报告有批量导入与清洗设计。
- [ ] 报告有触发器和存储过程设计。
- [ ] 报告有索引设计和执行计划分析。
- [ ] 报告有 TPC-H 统计分析。
- [ ] 报告有 TPC-C 事务分析。
- [ ] 报告有并发测试分析。
- [ ] 报告有系统运行截图。
- [ ] 报告有小组分工。
- [ ] 报告有总结。
- [ ] 代码作为单独附件。
- [ ] SQL 脚本作为单独附件。
- [ ] README 能指导老师或助教启动系统。

---

## 19. 最终一句话定位

本项目应定位为：

> 基于 SpringBoot + Vue 3 + PostgreSQL/openGauss 的 TPC Benchmark 电商数据管理系统，实现了 TPC-H 大规模数据管理、数据批量导入与清洗、业务查询与统计分析、TPC-C 事务处理、索引优化、执行计划分析和并发性能测试，完整覆盖数据库应用系统从需求分析、逻辑设计、物理设计到系统实现与性能评估的全过程。

验收时所有成员都要围绕这句话展开，不要把项目讲成普通后台管理系统。
